version: '3.8'

services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  # backend:
  #   build: ./backend
  #   ports:
  #     - "4000:4000"
  #   environment:
  #     MONGODB_URI: mongodb://mongo:27017/neurocloak
  #     RDB_URL: redis://redis:6379
  #   depends_on:
  #     - mongo
  #     - redis

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"

volumes:
  mongo_data:
